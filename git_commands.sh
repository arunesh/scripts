
# Force push to remote branch (such as Github) in order to force
# write its history.

git push -f -u origin exp_1

# If you have killed commits using git reset, use git reflog to get back to an older
# state.
#
#


# Track a remote branch.
git remote show origin
git remote add origin git@github.com:arunesh/createtts.git
